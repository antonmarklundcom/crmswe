// Meta Graph API version pin, shared by every caller in this module
// (webhook media download, send, template sync). Bumping the version is a
// one-line change here rather than a hunt across files.
export const GRAPH_API_BASE = "https://graph.facebook.com/v21.0";
